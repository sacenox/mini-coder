pre-commit:
  parallel: false
  commands:
    check:
      run: bun run --silent check
    format:
      run: bun run --silent format -- --log-level warn
    test:
      run: bun test --only-failures
    typecheck:
      run: bun run --silent typecheck
